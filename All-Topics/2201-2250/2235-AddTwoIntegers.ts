const num1 = -10,
  num2 = 4

/**
 * @param {number} num1
 * @param {number} num2
 * @return {number}
 */
var sum = function (num1: number, num2: number) {
  return num1 + num2
}

console.log(sum(num1, num2))
