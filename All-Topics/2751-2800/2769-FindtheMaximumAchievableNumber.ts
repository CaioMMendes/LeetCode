const num = 4,
  t = 1

/**
 * @param {number} num
 * @param {number} t
 * @return {number}
 */
var theMaximumAchievableX = function (num: number, t: number) {
  return num + t * 2
}

console.log(theMaximumAchievableX(num, t))
